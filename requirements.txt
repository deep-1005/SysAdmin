# ── Desktop GUI ──────────────────────────────────
PyQt6>=6.6.0

# ── System monitoring ────────────────────────────
psutil>=5.9.0

# ── AI: CrewAI runs on Python 3.12 with local Ollama ──
crewai>=1.14.1
# Ollama is still used as the local model server via HTTP to localhost:11434
# Use Python 3.12 for the CrewAI runtime in this repo

# ── Env / secrets ────────────────────────────────
python-dotenv>=1.0.0
prometheus-client>=0.20.0